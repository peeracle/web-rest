{
  "name": "peeracle-web-rest",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=prod nodemon ./bin/www",
    "test": "jasmine-node --verbose test"
  },
  "main": "./bin/www",
  "engines": {
    "node": "0.12.x"
  },
  "dependencies": {
    "body-parser": "~1.0.0",
    "express": "~4.2.0",
    "mysql": "^2.7.0",
    "orm": "^2.1.26"
  },
  "devDependencies": {
    "supertest": "^1.0.1"
  }
}
